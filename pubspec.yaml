name: profile_page
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0

environment:
  sdk: ^3.8.0

dependencies:
  flutter:
    sdk: flutter
  permission_handler: ^12.0.0   # versión estable de abril‑2025 
  geolocator: ^14.0.1  # Para la posición real 
  google_maps_flutter: ^2.12.3

dev_dependencies:
  flutter_lints: ^5.0.0
  flutter_test:
    sdk: flutter

flutter:
  uses-material-design: true
  assets:
    - assets/
